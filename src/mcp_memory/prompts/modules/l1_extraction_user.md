输出语言：根据【待提取的新消息】中 user 发言的主导语言书写 `scene_name` 和 memory `content`。

【上一个情境】：
{previous_scene_name}

【背景消息】（仅供理解上下文、关系和时间，严禁从这里提取记忆）：
{background_messages}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【待提取的新消息】（只从这里提取记忆；务必结合 timestamp 推算时间）：
{new_messages}
